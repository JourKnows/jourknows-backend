# render.yaml

services:
  # ==========================================
  # 1. PRODUCTION ENVIRONMENT (Watches: main)
  # ==========================================
  - type: web
    name: jourknows-api-prod
    runtime: docker
    branch: main
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        value: https://jourknows-frontend-prod.onrender.com
      - key: JWT_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false

  # ==========================================
  # 2. STAGING ENVIRONMENT (Watches: develop)
  # ==========================================
  - type: web
    name: jourknows-api-staging
    runtime: docker
    branch: develop
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: CORS_ORIGIN
        value: https://jourknows-frontend-staging.onrender.com
      - key: JWT_SECRET
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: REDIS_URL
        sync: false

